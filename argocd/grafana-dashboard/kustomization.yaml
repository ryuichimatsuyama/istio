apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: frontend-slo-dashboard
    namespace: monitoring
    files:
      - slo-dashboard.json

generatorOptions:
  labels:
    grafana_dashboard: "1"
